version: "3.4"

services:
  uptime-kuma:
    image: louislam/uptime-kuma:2
    container_name: uptime-kuma
    volumes:
      - ./uptime_data:/app/data
    ports:
      - "127.0.0.1:3002:3001"
    restart: unless-stopped
